#include <stdio.h> 
int main()
{
    float radius ;
    printf("Enter the radius of the circle");
    scanf("%f",&radius);
   float area = 3.14 * radius * radius;
    printf("area of circle =%f", area);
    return 0;
}
